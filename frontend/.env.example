# Frontend environment example for Vite
# Copy to .env and update values as needed

# Backend API base URL used by the frontend (must include protocol)
VITE_BACKEND_URL=http://localhost:3001

# Socket URL (optional, defaults to VITE_BACKEND_URL)
VITE_SOCKET_URL=http://localhost:3001
